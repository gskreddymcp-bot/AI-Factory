# 03 Security

- Row-level security enabled on all tenant data tables.
- Server-side payload validation via Zod.
- Fastify route rate limits enabled for abuse prevention.
- Secrets loaded from environment variables only.
